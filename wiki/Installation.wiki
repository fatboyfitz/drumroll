#Installation and Configuration

= Installation =

Unpack the install

`tar -xjf drumroll-X.x.tbz2`

Build the app

`make`

Install the app

`make install`

NOTE: Application will install into /usr/bin

= Configuration =

You can change drum sounds by changing the 'pad#.wav' symlinks in samples directory